name: CI/CD to EC2

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.HOST_ACCESS_ID }}
        aws-secret-access-key: ${{ secrets.HOST_SECRET_ACCESS_KEY }}
        aws-region: ap-south-1

   
        script: |
          cd /path/to/app
          git pull origin main
          docker compose down || true
          docker build -t system-health-app .
          docker compose up -d
